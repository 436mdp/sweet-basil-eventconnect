-- Optional seed data for development
-- Run after migrations and after creating an admin user

-- Example: set admin role (replace email)
-- UPDATE profiles SET role = 'admin' WHERE email = 'admin@sweetbasil.com';
